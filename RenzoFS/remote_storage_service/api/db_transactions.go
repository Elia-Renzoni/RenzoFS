package api

/**
* quando un utente inserisce una nuova cartella devo vedere se l'utente esiste
* se non esiste allora blocco l'operazione, se esiste invece creo la cartella
* e aggiungo la cartella all'utente.
*
*
* quando un utente legge/scrive/modifica/elimina un file allora devo controllare
* se il suo nome utente coincide con gl amici dell'utente che ha creato la cartella
* anche per le informazioni statistiche.
 */

type DatabaseTransactions struct {
}
